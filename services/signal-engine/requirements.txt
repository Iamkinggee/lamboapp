# services/signal-engine/requirements.txt
# SMC Trading SaaS — Phase 2 (Signal Engine)
# Install: pip install -r requirements.txt

websockets==12.0
redis==5.0.1
httpx==0.26.0
python-dotenv==1.0.0
# Optional but recommended
uvloop==0.19.0          # Faster asyncio event loop (Linux/macOS only)